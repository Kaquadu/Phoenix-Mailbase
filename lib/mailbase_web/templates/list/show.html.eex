<h1>Pokaż parametry listy</h1>

<ul>

  <li>
    <strong>Nazwa:</strong>
    <%= @list.name %>
  </li>

  <li>
    <strong>Desc</strong>
    <%= @list.desc %>
  </li>


  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>

        <th></th>
      </tr>
    </thead>
    <tbody>
  <%= for address <- @addresses do %>
      <tr>
        <td><%= address.name %></td>
        <td><%= address.email %></td>

        <td>
          <%= link "Edytuj", to: Routes.address_path(@conn, :edit, address, list_id: @list.id) %>
          <%= link "Usuń", to: Routes.address_path(@conn, :delete, address), method: :delete, data: [confirm: "Are you sure?"] %>
        </td>
      </tr>
  <% end %>

<span><%= link "Wróć", to: Routes.list_path(@conn, :index) %></span> |
<span><%= link "Edytuj", to: Routes.list_path(@conn, :edit, @list) %></span> |
<span><%= link "Dodaj odbiorcę", to: Routes.list_path(@conn, :add_receiver, list_id: @list.id) %></span>
